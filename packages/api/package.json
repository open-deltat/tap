{
	"name": "@open-tap/api",
	"version": "0.1.0",
	"private": true,
	"description": "TAP v0.1 HTTP API with embedded SQLite",
	"type": "module",
	"main": "./src/index.ts",
	"scripts": {
		"dev": "bun run --watch src/index.ts",
		"start": "bun run src/index.ts",
		"test": "bun test",
		"test:watch": "bun test --watch",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@open-tap/core": "workspace:*",
		"@open-tap/protocol": "workspace:*",
		"drizzle-orm": "^0.36.0",
		"ulid": "^3.0.1"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
