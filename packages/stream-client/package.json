{
	"name": "@tap/stream-client",
	"version": "0.1.0",
	"description": "TAP v0.1 Stream Client - Client library for consuming TAP event streams",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"clean": "rm -rf dist",
		"typecheck": "tsc --noEmit",
		"test": "bun test",
		"test:watch": "bun test --watch",
		"lint": "biome check .",
		"format": "biome format --write .",
		"lint:fix": "biome check --write ."
	},
	"dependencies": {
		"@tap/core": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
